# 后端开发规划：AI Agent 日程与任务协同助手

## 总体开发顺序（5 个阶段，自底向上）

```
阶段1: 项目骨架       阶段2: 数据层        阶段3: 业务层         阶段4: AI Agent      阶段5: 接口与联调
┌──────────┐       ┌──────────┐       ┌──────────┐        ┌──────────────┐     ┌──────────────┐
│ Maven工程 │  ──▶  │ Entity   │  ──▶  │ 用户服务  │  ──▶   │ LLM Client   │ ──▶ │ Controller   │
│ SpringBoot│       │ Mapper   │       │ 目标服务  │        │ IntentParser │     │ 全局异常处理  │
│ 基础配置   │       │ 枚举/DTO │       │ 任务服务  │        │ AgentOrch.   │     │ Knife4j文档  │
│ Security  │       │          │       │ 日程服务  │        │ 工具注册     │     │ 接口测试     │
│ Redis     │       │          │       │ 通知服务  │        │ 执行追踪     │     │              │
└──────────┘       └──────────┘       └──────────┘        └──────────────┘     └──────────────┘
```

---

## 阶段 1：项目骨架搭建

### 1.1 创建 Maven 工程

```
创建 SpringBoot 3.x 项目
├── 引入依赖: spring-boot-starter-web, mybatis-plus, mysql-connector,
│            spring-security, redis, lombok, hutool, knife4j, springdoc
├── application.yml: 数据源、Redis、端口、日志级别
├── 启动类: ScheduleApplication.java
└── 跑通启动验证
```

### 1.2 基础配置类

| 类 | 说明 |
|----|------|
| `MyBatisPlusConfig` | 分页插件、逻辑删除 |
| `RedisConfig` | 序列化配置 |
| `WebMvcConfig` | CORS 跨域配置 |
| `JacksonConfig` | JSON 日期格式化、时区 |

### 1.3 公共组件

| 类 | 说明 |
|----|------|
| `common/response/ApiResponse<T>` | 统一响应体 `{code, message, data}` |
| `common/response/PageResult<T>` | 分页响应 |
| `common/exception/BusinessException` | 业务异常 |
| `common/exception/GlobalExceptionHandler` | `@RestControllerAdvice` 全局异常处理 |
| `common/enums/ResultCode` | 错误码枚举 |

### 1.4 安全模块

```
security/
├── JwtTokenProvider        # JWT 签发与校验
├── JwtAuthenticationFilter  # OncePerRequestFilter
├── SecurityConfig           # SecurityFilterChain + 放行白名单
└── UserDetailsServiceImpl   # 从DB加载用户
```

**验证方式**：写一个 `/api/v1/hello` 接口，登录后带 Token 访问成功。

---

## 阶段 2：数据层

### 2.1 Entity（实体类）

按顺序创建，对应 9 张表：

| 序号 | Entity | 对应表 |
|------|--------|--------|
| 1 | `User` | user |
| 2 | `Goal` | goal |
| 3 | `Task` | task |
| 4 | `Schedule` | schedule |
| 5 | `Reminder` | reminder |
| 6 | `AgentLog` | agent_log |
| 7 | `AgentConfig` | agent_config |
| 8 | `PromptTemplate` | prompt_template |
| 9 | `NotificationLog` | notification_log |

每张表统一使用 `@TableName`、`@TableId(type=IdType.AUTO)`，日期用 `LocalDateTime`。

### 2.2 Mapper 接口

每个 Entity 对应一个 Mapper，继承 `BaseMapper<T>`：

```java
public interface UserMapper extends BaseMapper<User> {}
public interface GoalMapper extends BaseMapper<Goal> {}
// ... 其余同理
```

### 2.3 枚举类

| 枚举 | 值 |
|------|-----|
| `GoalStatus` | PENDING, CLARIFYING, PARSED, PLANNED, CONFIRMED, CANCELLED |
| `TaskStatus` | TODO, IN_PROGRESS, DONE, DELAYED, CANCELLED |
| `TaskPriority` | HIGH(1), MEDIUM(2), LOW(3) |
| `ReminderChannel` | IN_APP, EMAIL, WECHAT, FEISHU |

### 2.4 DTO / VO

| 用途 | 存放位置 |
|------|----------|
| 接口入参 | `model/dto/`，如 `GoalParseDTO`, `PlanConfirmDTO` |
| 接口出参 | `model/vo/`，如 `GoalParseVO`, `TaskDetailVO` |
| Agent 内部用 | `model/agent/`，如 `ParseResult`, `SubTask`, `Conflict`, `PlanResult` |

### 2.5 验证方式

写一个 `UserMapperTest`，连真实 MySQL 查询 admin 用户，断言 `username = "admin"`。

---

## 阶段 3：业务服务层

### 3.1 基础 CRUD 服务

每个 Service 先只做基础 CRUD，后续再叠加 Agent 逻辑：

| Service | 核心方法 |
|---------|----------|
| `UserService` | findById, findByUsername |
| `GoalService` | create, updateStatus, updateParsedResult, incrementRound, getClarifyRound, savePartialResult |
| `TaskService` | create, update, updateStatus, delete, pageQuery |
| `ScheduleService` | create, batchCreate, findByUserAndRange |
| `ReminderService` | create, batchCreate, findPendingReminders |
| `AgentConfigService` | findByAgentType, listAll |
| `PromptTemplateService` | findByAgentTypeAndVersion |
| `NotificationService` | send, retryFailed |

### 3.2 关键方法实现提示

**GoalService.incrementRound**：
```sql
UPDATE goal SET clarify_round = clarify_round + 1 WHERE id = ?
```

**ScheduleService.findByUserAndRange**（排期冲突检测用）：
```sql
SELECT * FROM schedule WHERE user_id = ? AND start_time >= ? AND end_time <= ? AND status = 'ACTIVE'
```

**ReminderService.findPendingReminders**（定时任务扫描用）：
```sql
SELECT * FROM reminder WHERE remind_time <= NOW() AND status = 'PENDING'
```

### 3.3 验证方式

写 `GoalServiceTest`：创建 goal → 状态为 PENDING → incrementRound → round=1 → updateStatus → 状态变更验证。

---

## 阶段 4：AI Agent 核心（最关键）

### 4.1 建包结构

```
service/agent/
├── AgentOrchestrator.java    # 编排器（入口）
├── IntentParser.java         # 意图识别（调用LLM）
├── TaskDecomposer.java       # 任务拆解（调用LLM）
├── ConflictDetector.java     # 冲突检测（查DB + LLM分析）
├── PlanRecommender.java      # 排期推荐（调用LLM）
├── ExecutionTracker.java     # 执行日志记录
├── LLMClient.java            # LLM HTTP 调用封装
├── ToolRegistry.java         # 工具注册中心
├── model/
│   ├── ParseResult.java      # 解析结果（含 needsClarification/completed 工厂方法）
│   ├── SubTask.java
│   ├── Conflict.java
│   ├── PlanResult.java
│   └── LLMResponse.java      # LLM 响应封装
└── tool/
    ├── ToolDefinition.java   # 工具定义模型
    ├── ToolExecutor.java     # 工具执行接口
    ├── QueryScheduleTool.java
    ├── DetectConflictTool.java
    ├── CreateTaskTool.java
    └── SetReminderTool.java
```

### 4.2 开发顺序

| 序号 | 模块 | 说明 | 可单独验证 |
|------|------|------|-----------|
| 1 | `LLMClient` | 封装 OpenAI 兼容 API 调用，传入 systemPrompt + userMessage，返回 JSON | 写 main 方法直接调 LLM API 看返回 |
| 2 | `IntentParser` | 用 LLMClient 调意图识别 Prompt 模板，解析返回 JSON | 输入一句话，输出结构化目标 |
| 3 | `AgentOrchestrator.processOneRound()` | 串联 IntentParser + 信息检查 + 追问逻辑 | 模拟一轮解析 |
| 4 | `ConflictDetector` | 调 ScheduleService 查已有日程 + 时间重叠计算 | 给一段候选时间，返回冲突列表 |
| 5 | `TaskDecomposer` | 根据解析后的目标拆解子任务 | 单独调 LLM |
| 6 | `PlanRecommender` | 输入子任务+冲突+截止时间 → 生成排期方案 | 单独调 LLM |
| 7 | `ToolRegistry` + 工具实现 | 注册 QuerySchedule/CreateTask/SetReminder 三个工具 | Controller 调 createTask 写库 |
| 8 | `ExecutionTracker` | 所有关键步骤写 agent_log 表 | 跑一轮流程看日志表 |
| 9 | `AgentOrchestrator` 全流程 | 串联 processOneRound → generatePlan → executePlan | 端到端流程 |

### 4.3 LLMClient 设计要点

```java
public class LLMClient {
    // 核心方法：发送 Chat Completion 请求
    // 入参：systemPrompt, userMessage, modelName
    // 出参：LLMResponse（含 content, tokenUsage, latencyMs）
    // 异常：超时 30s，重试 1 次

    // 配置：
    // - apiKey 从 application.yml 读取（llm.api-key）
    // - baseUrl 可配置，支持 OpenAI / 国产模型
    // - 默认 model 从 agent_config 表读取
}
```

### 4.4 阶段 4 验证方式

不连 Controller，在 Service 层写单元测试：
1. `IntentParser` 输入"帮我安排下周五新人培训汇报" → 返回 JSON 含目标和截止时间
2. 输入"下周汇报"（模糊） → 返回 confidence < 0.5 且 missingInfo 非空
3. `AgentOrchestrator.processOneRound` 模糊输入 → 返回 needsClarification=true
4. `AgentOrchestrator.processOneRound` 清晰输入 → 返回 completed=true + 完整解析结果

---

## 阶段 5：Controller 层与集成

### 5.1 Controller 开发顺序

| 序号 | Controller | 接口数 | 依赖的 Service |
|------|------------|--------|----------------|
| 1 | `AuthController` | 2 (login, refresh) | UserService, JwtTokenProvider |
| 2 | `GoalController` | 4 (parse, reply, result, confirm) | GoalService, AgentOrchestrator |
| 3 | `PlanController` | 3 (generate, preview, confirm) | GoalService, AgentOrchestrator, TaskService, ScheduleService, ReminderService |
| 4 | `TaskController` | 7 (CRUD + status + reschedule + logs) | TaskService |
| 5 | `DashboardController` | 2 (dashboard, report) | TaskService, ScheduleService |
| 6 | `ReminderController` | 4 (CRUD) | ReminderService |
| 7 | 后台管理 Controller | ~12 个 | Admin 相关 Service |

### 5.2 Controller 规范

```java
@RestController
@RequestMapping("/api/v1/goals")
@Tag(name = "目标管理")
public class GoalController {

    @PostMapping("/parse")
    @Operation(summary = "提交自然语言目标")
    public ApiResponse<GoalParseVO> parse(@RequestBody @Valid GoalParseDTO dto) {
        // 1. 创建 goal
        // 2. 调 orchestrator.processOneRound()
        // 3. 返回解析进度
    }

    @PostMapping("/{id}/reply")
    @Operation(summary = "回复Agent追问")
    public ApiResponse<GoalParseVO> reply(@PathVariable Long id,
                                           @RequestBody @Valid GoalReplyDTO dto) {
        // 1. 追加对话历史到 goal.conversation
        // 2. 调 orchestrator.processOneRound()
        // 3. 返回新一轮解析结果
    }
}
```

### 5.3 全局配置收尾

- `application.yml` 添加 Knife4j / SpringDoc 配置
- 所有 Controller 添加 `@Tag` + `@Operation` 注解
- 启动后访问 `http://localhost:8080/doc.html` 查看 Swagger 文档
- `GlobalExceptionHandler` 覆盖所有已知异常

### 5.4 验证方式

用 Postman 或 Knife4j 逐接口测试：
1. `POST /api/v1/auth/login` → 获取 Token
2. `POST /api/v1/goals/parse` → 提交目标，观察 Agent 返回
3. 如果返回追问 → `POST /api/v1/goals/{id}/reply` → 回复
4. 循环 2-3 到解析完成 → `POST /api/v1/goals/{id}/plan` → 获取排期
5. `POST /api/v1/goals/{id}/plan/confirm` → 批量写入 task/schedule/reminder
6. 检查 `agent_log` 表有完整记录

---

## 开发顺序总览（建议按天推进）

```
Day 1: 阶段1 项目骨架
  ├── Maven工程 + SpringBoot启动
  ├── 基础配置类（MyBatis-Plus, Redis, CORS, Jackson）
  ├── 公共组件（ApiResponse, GlobalExceptionHandler）
  └── Security（JWT + 登录接口）

Day 2: 阶段2 数据层
  ├── 9个Entity + 8个Mapper
  ├── 4个枚举类
  ├── DTO/VO（按接口清单逐步补充）
  └── 验证：UserMapperTest 通过

Day 3: 阶段3 业务层（基础CRUD）
  ├── GoalService, TaskService, ScheduleService
  ├── ReminderService, NotificationService
  ├── AgentConfigService, PromptTemplateService
  └── 验证：GoalServiceTest 通过

Day 4: 阶段4 AI Agent 核心（上）
  ├── LLMClient（封装HTTP调用）
  ├── IntentParser（意图识别）
  ├── AgentOrchestrator.processOneRound()（带追问的单轮处理）
  └── 验证：LLM返回正常，追问循环逻辑正确

Day 5: 阶段4 AI Agent 核心（下）
  ├── TaskDecomposer, PlanRecommender（拆解+排期）
  ├── ConflictDetector（查日程+冲突计算）
  ├── ToolRegistry + 工具实现（CreateTask等）
  ├── ExecutionTracker（日志写入）
  └── AgentOrchestrator 全流程串联

Day 6: 阶段5 Controller层
  ├── GoalController, PlanController（核心链路）
  ├── TaskController, DashboardController
  ├── ReminderController
  ├── Knife4j文档配置
  └── 验证：Postman逐接口通过
```

---

## 项目结构最终形态

```
src/main/java/com/aspire/schedule/
├── ScheduleApplication.java
├── config/                    # 配置类
│   ├── MyBatisPlusConfig.java
│   ├── RedisConfig.java
│   ├── WebMvcConfig.java
│   ├── JacksonConfig.java
│   └── LLMClientConfig.java
├── security/                  # 安全
│   ├── JwtTokenProvider.java
│   ├── JwtAuthenticationFilter.java
│   ├── SecurityConfig.java
│   └── UserDetailsServiceImpl.java
├── controller/                # 接口层
│   ├── AuthController.java
│   ├── user/
│   │   ├── GoalController.java
│   │   ├── PlanController.java
│   │   ├── TaskController.java
│   │   ├── DashboardController.java
│   │   └── ReminderController.java
│   └── admin/
│       ├── AccountController.java
│       ├── AgentController.java
│       ├── PromptController.java
│       ├── ChannelController.java
│       └── SessionController.java
├── service/                   # 业务层
│   ├── UserService.java
│   ├── GoalService.java
│   ├── TaskService.java
│   ├── ScheduleService.java
│   ├── ReminderService.java
│   ├── NotificationService.java
│   ├── AgentConfigService.java
│   ├── PromptTemplateService.java
│   ├── agent/                 # Agent 核心
│   │   ├── AgentOrchestrator.java
│   │   ├── IntentParser.java
│   │   ├── TaskDecomposer.java
│   │   ├── ConflictDetector.java
│   │   ├── PlanRecommender.java
│   │   ├── ExecutionTracker.java
│   │   ├── LLMClient.java
│   │   └── ToolRegistry.java
│   ├── agent/model/
│   │   ├── ParseResult.java
│   │   ├── SubTask.java
│   │   ├── Conflict.java
│   │   ├── PlanResult.java
│   │   └── LLMResponse.java
│   └── agent/tool/
│       ├── ToolDefinition.java
│       ├── ToolExecutor.java
│       ├── QueryScheduleTool.java
│       ├── DetectConflictTool.java
│       ├── CreateTaskTool.java
│       └── SetReminderTool.java
├── repository/                # 数据层
│   ├── mapper/
│   │   ├── UserMapper.java
│   │   ├── GoalMapper.java
│   │   ├── TaskMapper.java
│   │   ├── ScheduleMapper.java
│   │   ├── ReminderMapper.java
│   │   ├── AgentLogMapper.java
│   │   ├── AgentConfigMapper.java
│   │   ├── PromptTemplateMapper.java
│   │   └── NotificationLogMapper.java
│   └── entity/
│       ├── User.java
│       ├── Goal.java
│       ├── Task.java
│       ├── Schedule.java
│       ├── Reminder.java
│       ├── AgentLog.java
│       ├── AgentConfig.java
│       ├── PromptTemplate.java
│       └── NotificationLog.java
├── model/                     # 数据传输对象
│   ├── dto/                   # 入参
│   ├── vo/                    # 出参
│   └── enums/                 # 枚举
└── common/                    # 公共组件
    ├── response/
    │   ├── ApiResponse.java
    │   └── PageResult.java
    ├── exception/
    │   ├── BusinessException.java
    │   └── GlobalExceptionHandler.java
    └── util/
```

---

## 关键提醒

1. **先跑通再完善**：每个阶段结束时保证可以编译启动，不要等到最后才集成
2. **阶段 4 是核心难点**：LLM 调用不稳定，建议先 mock 返回 JSON 把流程跑通，再接入真实 API
3. **测试用户**：预置的 admin/demo（密码 123456）可直接用于登录调试
4. **MySQL 连接**：`jdbc:mysql://localhost:3306/ai_schedule?characterEncoding=utf8mb4`
5. **日志先行**：ExecutionTracker 从阶段 3 就开始记录，不要等到阶段 4 才补
